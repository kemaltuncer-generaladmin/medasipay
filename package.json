{
  "name": "medasipay",
  "version": "1.0.0",
  "private": true,
  "description": "MedAsi bank transfer checkout and order tracking service.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check assets/app.js"
  }
}
